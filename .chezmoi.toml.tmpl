{{- /* chezmoi config — generated on each `chezmoi init` from this template.
   age encryption with a key file (chezmoi's recommended default).
   The private key (key.txt) lives at ~/.config/chezmoi/key.txt and is NEVER
   committed — copy it to a new machine (from your password manager) before
   running `chezmoi init --apply harrisonstropkay/dotfiles`.
   The recipient below is PUBLIC; it's safe in the repo. */ -}}
encryption = "age"

[age]
    identity = "{{ .chezmoi.homeDir }}/.config/chezmoi/key.txt"
    recipient = "age174nvtj5gfpar5v894t4ar3krq94nzm8p4wdy5cdum7cugzl05y2qlsxhu6"
